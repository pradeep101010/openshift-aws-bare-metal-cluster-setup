apiVersion: machine.openshift.io/v1beta1
kind: MachineSet
metadata:
  name: worker-autoscale
  namespace: openshift-machine-api
  labels:
    machine.openshift.io/cluster-api-cluster: ${cluster_name}
  annotations:
    machine.openshift.io/cpu: "96"
    machine.openshift.io/memory: "393216Mi"
    machine.openshift.io/gpu-count: "0"
    machine.openshift.io/gpu-type: ""
spec:
  replicas: ${worker_count}
  selector:
    matchLabels:
      machine.openshift.io/cluster-api-machineset: worker-autoscale
  template:
    metadata:
      labels:
        machine.openshift.io/cluster-api-machineset: worker-autoscale
        machine.openshift.io/cluster-api-cluster: ${cluster_name}
    spec:
      providerSpec:
        value:
          apiVersion: machine.openshift.io/v1beta1
          kind: None